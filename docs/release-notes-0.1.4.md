# Suwol Tools 0.1.4

## 수정 사항

- Electron production Renderer asset 경로를 `loadFile()`에 맞는 상대 경로로 수정했습니다.
- 기존 `0.1.3` 설치본에서 발생하던 실행 후 흰 화면 문제를 해결했습니다.
- Electron 앱 번들에서 JavaScript, CSS, lazy-loaded feature chunk가 정상적으로 로드됩니다.
