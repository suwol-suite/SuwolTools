# Suwol Tools release

This release includes the Windows NSIS/portable builds, Linux AppImage/tar.gz build, and platform update metadata where supported.

See `checksums.txt` and the detached GPG signature for download verification.
